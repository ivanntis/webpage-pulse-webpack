import { EventEmitter } from '../../../stencil.core';
import { Color, ColorVariant } from '../../../interface';
export declare class PulseTab {
    private inputId;
    color: Color;
    colorvariant: ColorVariant;
    disabled: boolean;
    tabtext: string;
    value?: any | null;
    selected: boolean;
    /**
     * Emitted when the select card is selected.
     */
    tabSelect: EventEmitter<any>;
    /**
     * Emitted when selected select card is selected.
     * @internal
     */
    tabDeselect: EventEmitter<any>;
    /**
 * Emitted when the radio loads.
 * @internal
 */
    tabDidLoad: EventEmitter<void>;
    /**
     * Emitted when the radio unloads.
     * @internal
     */
    tabDidUnload: EventEmitter<void>;
    selectChanged(isSelected: boolean): void;
    constructor();
    componentDidLoad(): void;
    componentDidUnload(): void;
    componentWillLoad(): void;
    private onClick;
    hostData(): {
        class: {
            'button-disabled': boolean;
            'pulse-focusable': boolean;
            'tab-select--selected': boolean;
        };
    };
    render(): any;
}
