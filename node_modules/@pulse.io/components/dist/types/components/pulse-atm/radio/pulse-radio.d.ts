import { EventEmitter } from '../../../stencil.core';
import { Color } from '../../../interface';
export declare class PulseRadio {
    private inputId;
    disabled: boolean;
    label: string;
    name: string;
    value?: any | null;
    checked: boolean;
    color: Color;
    valueEmmit: EventEmitter<boolean>;
    /**
     * Emitted when the radio button is selected.
     */
    radioSelect: EventEmitter<any>;
    /**
     * Emitted when checked radio button is selected.
     * @internal
     */
    radioDeselect: EventEmitter<any>;
    /**
     * Emitted when the radio loads.
     * @internal
     */
    radioDidLoad: EventEmitter<void>;
    /**
     * Emitted when the radio unloads.
     * @internal
     */
    radioDidUnload: EventEmitter<void>;
    constructor();
    componentWillLoad(): void;
    hostData(): {
        class: {
            [`radio-disabled`]: boolean;
            [`radio-disabled-checked`]: boolean;
        };
    };
    checkedChanged(isChecked: boolean): void;
    componentDidLoad(): void;
    componentDidUnload(): void;
    private onClick;
    render(): any;
}
