import { ComponentInterface } from '../../../stencil.core';
export declare class ToolTipController implements ComponentInterface {
    present<T>(properties: any): Promise<T>;
    dismiss<T>(id: any): Promise<T>;
}
