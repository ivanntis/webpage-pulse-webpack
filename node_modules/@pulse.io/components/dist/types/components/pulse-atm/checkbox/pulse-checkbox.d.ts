import { EventEmitter } from '../../../stencil.core';
import { Color } from '../../../interface';
export declare class PulseCheckBox {
    disabled: boolean;
    name: string;
    value: string;
    checked: boolean;
    color: Color;
    valueEmmit: EventEmitter<boolean>;
    constructor();
    hostData(): {
        class: {
            [`checkbox-disabled`]: boolean;
            [`checkbox-disabled-checked`]: boolean;
        };
    };
    handleCheck(event: any): void;
    render(): any;
}
