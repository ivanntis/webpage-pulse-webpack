import { EventEmitter } from '../../../stencil.core';
import { Color } from '../../../interface';
export declare class PulseSelectCard {
    private inputId;
    color: Color;
    disabled: boolean;
    value?: any | null;
    selected: boolean;
    /**
     * Emitted when the select card is selected.
     */
    sCardSelect: EventEmitter<any>;
    /**
     * Emitted when selected select card is selected.
     * @internal
     */
    sCardDeselect: EventEmitter<any>;
    /**
 * Emitted when the radio loads.
 * @internal
 */
    sCardDidLoad: EventEmitter<void>;
    /**
     * Emitted when the radio unloads.
     * @internal
     */
    sCardDidUnload: EventEmitter<void>;
    selectChanged(isSelected: boolean): void;
    constructor();
    componentDidLoad(): void;
    componentDidUnload(): void;
    componentWillLoad(): void;
    private onClick;
    hostData(): {
        class: {
            'button-disabled': boolean;
            'pulse-focusable': boolean;
            'pulse-padding-m-xs-h': boolean;
            'pulse-padding-xs-xs-v': boolean;
            'btn-select--selected': boolean;
        };
    };
    render(): any;
}
